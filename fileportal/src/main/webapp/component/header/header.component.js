'use strict';

var headerComponent = {
    templateUrl: './app/component/header/header.html',
    bindings: {
        currentPage: '@',
    },

    controller: function () {
        function controller() {
        }

        return controller;
    }()
};